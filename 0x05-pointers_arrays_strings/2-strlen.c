#include "main.h"
/**
 * _strlen - returns length of string
 * @s: input parameter
 * Description: use pointers
 * Return: always (0)
 */
int _strlen(char *s)
{
	int a;

	for (a = 0; s[a] != '\0'; ++a)
	{
		continue;
	}

	return (a);
}

